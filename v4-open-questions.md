# v4 — Open Questions

> 这一份不是 todo 列表,是 v4 **主动承认还没闭合**的几个深层问题。它们之所以留在这里而不是被直接解掉,是因为每一个都需要超出"我一个人想清楚"的输入 —— 要么需要真实数据,要么需要多方讨论,要么需要超出当前知识边界。
>
> 这一份的存在本身就是 v4 设计原则 §11 的体现:**一个理论必须能宣告自己失败。同样,一个设计必须能宣告"这里我还不知道"**。
>
> 每一条都是 v5 的输入。

---

## 1. Replay test 的 "ground truth" 鸡生蛋问题

### 问题陈述

L4.5 的核心机制是:把 elicited rule 在历史 episode 上 replay,看预测的 action 和实际 action 是否一致。

但这里有一个隐藏假设:**员工历史 episode 里的实际 action 就是"正确答案"**。

这个假设在多数情况下成立,但在两种情况下崩溃:

1. **员工自己也犯过错**。他在过去某个 episode 里点了 Approve,但其实那一单应该被 Escalate。如果 L4.5 把这种"错误的过去"当成 ground truth,那它会**奖励一条同样犯错的规则**,惩罚一条正确的规则。
2. **员工的标准在漂移**。今天的他不会同意半年前的自己。L4.5 用半年前的 episode 验证今天的规则,会把"成长"误判为"规则不准"。

### 为什么 v4 没解

我能想到的几种"解":

- **方案 A**:让员工事后标注 "this episode I'd do differently now",显式排除。
  - 问题:成本高,而且员工不一定能识别自己的过去错误
- **方案 B**:用置信度加权 —— 越近的 episode 权重越高。
  - 问题:这只是缓解,不能解决"全部错误"的退化情形
- **方案 C**:把 L4.5 的 ground truth 从 "员工实际 action" 改为 "员工事后回看时的 action"。
  - 问题:这又把事后合理化引回来了 —— 这正是 v3→v4 极力避免的

### 这件事的真正难度

**这不是一个工程问题,是一个认识论问题**。任何 unsupervised 的规则归纳都会撞上这堵墙 —— 监督学习需要 label,而专家技能领域的 label 必须来自专家本人,而专家本人正是被研究的对象。

ACT-R 文献里也没人解决这个问题。Klein 的 CTA 方法论的解法是"让多个独立专家交叉验证",但这把成本拉到不可承受。

### v5 应该怎么开始想

- 接受 "L4.5 不是真理裁判,只是 confidence calibrator" — 它的输出不是 binary verified/unverified,是一个连续 confidence
- 把 ground truth 问题从 L4.5 内部挪到 L5 输出 — SKILL.md 不假装规则是对的,只标注 "此规则在 N 条历史 episode 上 hit 率 X%,N 条中员工事后认为有 K 条是误判"
- Phase 1 数据出来后,看真实场景里 "员工的过去错误" 这个问题严重到什么程度。如果不严重,就接受这个噪声;如果严重,需要 v5 引入显式的 "self-correction" 通道

---

## 2. epistemic_load 的主观漂移

### 问题陈述

v4 §4.2 让员工自己标 epistemic_load: low/medium/high。Phase 0 假设 A8 要测 IRR (inter-rater reliability)。

但即使 IRR 通过,有一个问题没解:**同一个员工在不同心情/不同压力下,会标出不同的 epistemic_load**。

更糟的是:**员工的标注会被系统的反馈强化**。如果他发现"标 high 的规则系统就不会替他做",他可能下意识地多标 high,以保留掌控感;反过来,如果他想偷懒,可能多标 low。

### 为什么 v4 没解

- 这不是测量问题,是博弈问题。员工知道自己的标注会影响系统行为,标注本身就不再是中立的
- A8 的 test-retest 设计能抓到第一类漂移(心情/压力),但抓不到第二类(策略性标注)

### v5 应该怎么开始想

- 把 epistemic_load 从 "员工自己标" 改成 "员工 + 一名同事 + 系统三方协商"
- 或者,把 high/medium/low 这三档从离散变成连续 —— 让员工拖一个 slider,而不是选三档,降低策略性标注的诱惑
- 最根本的方向:**让系统不要一次性决定 "automate vs remind"**,而是给一个**渐进试运行通道** —— 先 remind 一周,员工看到"哦原来系统会在这种时候提醒我",再决定要不要升级到 automate

---

## 3. 多员工版本的规则合并裁决

### 问题陈述

v3/v4 都默认 "一个员工一个 SKILL.md"。但真实场景里,同一个 task 可能被多个员工共同执行,每个人的判断标准略有不同。

如果系统聚合多个员工的归纳结果,会撞上:

- **直接冲突**:员工 A 说 "<50EUR 直接批",员工 B 说 "<30EUR 直接批"。谁对?
- **风格差异**:员工 A 偏保守,员工 B 偏激进。这不是错误,是合法的判断空间
- **资历差异**:新员工的归纳不应该和老员工平权
- **角色差异**:同岗位但 A 负责 VIP 客户,B 负责普通客户。规则应该按客户类型分,而不是按员工分

### 为什么 v4 没解

v3 把这一条列为 "Open Question",v4 也没动它。原因:任何"自动合并"方案都会把上面四种差异压平,失去信息;任何"人工裁决"方案都会把成本拉到 Phase 1 不可承受。

### v5 应该怎么开始想

- 不要试图合并。每个员工的 SKILL.md 独立存在,形成一个"规则联邦"
- Agent 调用时根据当前 task 上下文(客户类型、金额段、风险标签)选择最相关的某一份,而不是"merged 版本"
- 跨员工的"共识规则" 只在所有员工独立归纳出**几乎相同的 typed conditions** 时才合并 —— 这样避免风格压平
- 这等于把"合并"问题挪到了 Agent 调用时,由运行时上下文动态选择 —— 这又依赖 Agent 端有相应的 routing 能力,不在 Skill Harvester 范围内

---

## 4. UI 漂移检测的反向触发链路

### 问题陈述

v3/v4 的 L4 把 production 拆成 intent / execution 双层,UI 漂移时只需要重写 execution。

但有一个问题:**系统怎么知道 UI 漂移了?**

v3/v4 都依赖 Agent 在执行时报告失败,然后反向触发 L1 把这个失败标记为"困难瞬间"。但这条链路有几个洞:

1. Agent 不一定能识别"我失败了"。它可能点错按钮但流程继续,后果几小时后才显现
2. 反向链路要求 Agent 端和 Skill Harvester 端有持续的通信通道 —— 这违背 v4 §0.4 的规范前提(数据本地)
3. UI 漂移可能不是 "按钮位置变了" 这种突变,而是"按钮还在但语义变了"(例如 Approve 按钮新加了一步二次确认)。这种**语义漂移**没有任何技术信号能抓到

### 为什么 v4 没解

这是一个跨系统问题,牵涉 Skill Harvester / Agent / 目标系统三方,任何一方独立解都不够。

### v5 应该怎么开始想

- 给 verified rule 加一个 **"半衰期"** 字段:每条规则默认 90 天后自动降级回 hypothesis,需要重新 replay test
- 这是一个 "无信号也假设可能漂移" 的保守策略
- 配合一个 monthly health check:让员工定期回看 verified rules,主动标"这条还有效吗"

---

## 5. deployment manifest 在多机器同步场景下的一致性

### 问题陈述

v4 §10 把规范前提落到 `deployment_mode.yaml` 上。但员工可能在多台机器上工作(笔记本 + 桌面 + 手机)。

如果 manifest 在不同机器上不一致,系统行为不一致 → 规范前提的"5 项控制权"就漏了。

更糟:如果云端同步把 manifest 也同步了,manifest 本身就成为雇主可访问的数据,规范前提失效。

### 为什么 v4 没解

任何同步方案都涉及"数据出本机"这一动作,而 v4 §0.4 的硬约束是数据本地。

### v5 应该怎么开始想

- manifest 的同步必须**显式员工授权**,默认不同步
- 如果同步,只同步 manifest 本身,不同步 episodes / productions / SKILL.md
- 或者更激进:不允许 manifest 同步。每台机器独立部署,员工负责手动配置一致

---

## 6. ★ v4.1 替换:essence.md 与 v4.1 design 的措辞断层

> **v4.1 修订说明**:原第 6 条"v4 文档可读性 vs 严谨性的元问题"在 v4.5 引入 essence.md 之后已经自动消解 — essence 负责动人,v4/v4.1 负责严谨,分工已经实现。
>
> 但 essence + v4.1 的双层结构带来了一个新问题,记录在这里作为新的第 6 条。

essence.md 用员工视角的动作语言写:"它在角落亮一下,你可以不理"。
v4.1 §4.5.1 用工程语言写:"immediate 层 0-30s 自愿触发的红点"。

两者**意思一样**,但风格截然不同。员工先读 essence,再点进 v4.1 时,会经历一次明显的语义断层 —— 同一件事在两份文档里听起来像两个项目。

**为什么这是个真问题**:
- essence 的目标读者是被 Skill Harvester 采集的员工(目标用户)
- v4.1 的目标读者是工程师(实施者)
- 但**两者经常是同一个人**:员工出于好奇可能读 v4.1,工程师出于责任必须读 essence
- 两份文档的措辞如果完全独立演化,迟早会出现实质内容矛盾,而不只是风格不同

**v5 应该怎么开始想**:
- 不要试图统一风格 — 那会让 essence 变冷或让 v4.1 变软,两边都受损
- 但需要一份 **glossary** 把两份文档的关键词对照起来:essence 的"亮一下" = v4.1 的"immediate hotkey trigger";essence 的"角落" = v4.1 的"system tray notification";essence 的"那一下" = v4.1 的"surprisal episode"
- glossary 由 essence 这一侧主动维护(essence 是不动点),v4.1 修订时如果引入新术语,必须在 glossary 里和 essence 的对应词建立映射

---

## 7. ★ v4.1 新增:L4.5 改名为 Consistency Checker 之后,"假设检验闭环"这个叙事不再成立

v4 把 L4.5 命名为 Hypothesis Validator,配套叙事是"对话产生假设 → replay test 验证 → verified rule"。这个闭环很漂亮,但它依赖一个 v4 没解决的前提:replay test 能验证。

v4.1 把 L4.5 改名为 Consistency Checker,承认它**只能找出不一致的规则,不能证明一致的规则是对的**。这是诚实的修订,但它把 v4 的核心叙事卸掉了。

**新问题**:**v4.1 现在没有"verification"叙事了**。SKILL.md 里的 verified 段意味着什么?在 v4 里它的意思是"通过了 replay test 所以可信",在 v4.1 里它的意思变成"没被检测到不一致" —— 这是**否定式**的语义,比"通过验证"弱得多。

读者(尤其是非技术读者)看到 SKILL.md 里的 "verified" 标签会自然理解成强语义,但 v4.1 实际给的是弱语义。这是一个**潜在的承诺过载**。

**v5 应该怎么开始想**:
- 把 "verified" 重命名为 "consistent" 或 "self-consistent",诚实反映 v4.1 的语义
- 或者把 SKILL.md 的状态从二元 (verified/hypothesis) 改为连续 (consistency_score: 0.0-1.0),让读者自己看分数,不靠标签
- 这是一个**命名诚实**问题,影响下游 Agent 怎么解释规则,影响员工对系统的信任校准

---

## 8. ★ v4.1 新增:override 流程把 deployment manifest 变成"对抗系统",可能反向激励错误使用

v4.1 §10.3 加了 override 流程:员工可以通过显式命令 + reason + 红色横幅来违反 manifest 默认值。

这个设计的初衷是"留一个出口",但它可能产生反向激励:

- **场景**:某个团队部署后,leader 想要打开 use_for_evaluation。他知道 override 流程,他让员工跑那条命令,理由写"team trial"
- **结果**:员工面对 leader 的压力跑了 override,系统进入 degraded ethical mode,但**所有红色横幅、audit log、5 秒等待都没有阻止这件事发生**,只是让员工在事后能"证明我是被迫的"
- **问题**:override 流程的存在让"违反 manifest"变成一个**正常工作流**,而不是异常事件。这反而削弱了规范前提的力量

**v5 应该怎么开始想**:
- override 流程是不是应该**不存在**?让违反默认 = 系统永久拒绝启动,逼迫部署方在制度层面解决,而不是技术层面绕开
- 或者:override 必须有**冷却期**(例如 24 小时),让员工有时间在压力之外做决定
- 或者:override 必须有**外部签名**(例如 Betriebsrat 数字签名),不能由员工本人单独完成

这是一个**伦理 vs 实用**的真实张力,v4.1 选了实用,但代价可能比看起来大。

---

## 怎么用这一份文档

- 这 8 个问题任何一个有进展(数据 / 讨论 / 新洞察) → 在 [CHANGELOG.md](./CHANGELOG.md) 顶部新增一个版本章节,记录 "open question N 现在的回答"
- 这 8 个问题不是平权的。**第 1 条(replay ground truth)是最基础的**,它如果撑不住,L4.5 整个失效。**第 7 条(verified 命名诚实)和第 8 条(override 反向激励)是 v4.1 自己新引入的**,优先级低于 1-5
- 任何一个问题被新的批评推翻 → 直接把那条批评写进这份文档,不要试图反驳

每写一份新版设计文档,都应该带一份相同结构的 open-questions.md。**永远不要假装所有问题都被回答了**。
