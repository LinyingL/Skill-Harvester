# Skill Harvester — 项目设计文档

> **一句话定位**：一个把员工脑子里沉默的产生式（productions）"逼"出来、写下来、编译成可执行 SKILL.md 的认知任务分析自动化系统。
>
> **不是什么**：不是 RPA、不是录屏工具、不是 task mining。
>
> **是什么**：是 `skill-creator` 的上游——前者把已知知识写成 Skill，后者把未知知识变成已知。

---

## 1. 问题陈述

### 1.1 表层问题
要让 AI Agent 学会一个领域专家的工作方式，最直接的想法是"录下专家在电脑上的所有操作然后训练模型"。但这条路有三个致命问题：

1. **绝大多数行为没有信息量**——每天的鼠标点击 99% 是套路动作，记录它们是浪费比特。
2. **工作流中夹杂大量琐碎动作**——切窗口、复制粘贴、滚动浏览，这些是噪声不是信号。
3. **录屏存储成本爆炸**——一天 8 小时的高分辨率录屏可达数十 GB。

### 1.2 深层问题
即使解决了存储问题，纯行为录制也学不到专家的真正技能，因为：

- **专家最值钱的知识恰恰是他自己说不清的那部分**（Polanyi: *We know more than we can tell*）。
- 这部分知识在 ACT-R 框架里叫**程序性产生式**（procedural productions），它已经从陈述性记忆下沉到自动化执行，连专家自己都无法自省。
- 行为流只能记录产生式的"右手边"（动作），完全丢失了"左手边"（条件），所以无法泛化。

### 1.3 真正要解决的问题
**如何用最小的采集开销，最大化地还原员工脑子里的 IF-THEN 决策规则集，并把它编译成可被 Agent 直接调用的 SKILL.md？**

---

## 2. 核心设计原则

整个系统的所有设计选择都源自以下五条原则。每一条都来自认知科学已经验证的人脑机制。

### 2.1 信息 = 惊讶度（Shannon / Predictive Coding）
信息量等于 `-log p(事件)`。可预测的行为携带零信息。
**推论**：存储量应正比于 surprisal，而不是正比于时间。

### 2.2 困难是黄金信号（Klein RPD / Dreyfus）
专家的隐性知识只在遇到异常时才被迫"浮"回工作记忆做显式推理。
**推论**：盯着停顿、撤销、外部查询、反复切屏的瞬间——那是产生式唯一一次主动浮出水面的时刻。

### 2.3 条件不能被观察，只能被询问（Polanyi / CTA）
IF-THEN 规则的左手边存在于员工的工作记忆里，永远不会出现在屏幕上。
**推论**：必须有一个**反事实对话层**，主动用追问把隐性条件外化成语言。

### 2.4 奥卡姆剃刀的算法形式 = MDL
最优规则集是"规则总长度 + 用规则解释行为的修正长度"之和最小的那个。
**推论**：归纳产生式时优先选短规则、高覆盖率规则；不足以覆盖多个案例的规则要被剃掉。

### 2.5 机器编译必须保留可读性
人脑的 compilation 是单向不可逆的（这就是 tacit knowledge 的来源）；机器的 compilation 必须是双向的、可审计的、可被人类专家修订的。
**推论**：最终产物必须是 Markdown 形式的 SKILL.md，而不是黑盒模型权重。

---

## 3. 系统架构

### 3.1 五层架构总览

```
┌─────────────────────────────────────────────────┐
│  L5  Skill Compiler          → SKILL.md         │  产物层（KB级）
├─────────────────────────────────────────────────┤
│  L4  Production Inducer      → IF-THEN 规则集    │  归纳层（KB级）
├─────────────────────────────────────────────────┤
│  L3  Dialogue Engine         → 反事实问答记录     │  外化层（MB级）
├─────────────────────────────────────────────────┤
│  L2  Episode Builder         → 困难瞬间快照       │  采样层（MB级）
├─────────────────────────────────────────────────┤
│  L1  Sparse Sensor           → 语义边界事件流     │  感知层（KB/天）
└─────────────────────────────────────────────────┘
```

**设计要点**：每一层的数据量比下一层少 1-3 个数量级。这是奥卡姆剃刀的硬性约束——任何一层的数据膨胀都意味着设计失败。

---

### 3.2 L1 — Sparse Sensor（稀疏感知层）

**职责**：以最低开销持续监听用户工作环境，只产出语义边界事件。

**默认状态：沉默。** 仅在以下五类事件触发时输出一条记录。

| 事件类型 | 示例 | 含义 |
|---|---|---|
| 任务边界 | 打开新工单、收到新邮件、切换项目 | 新目标开始 |
| 决策边界 | 表单提交、文件保存、确认对话框 | 决策落地 |
| **异常边界** ⭐ | 错误对话框、撤销、长停顿（>5s）、反复切窗口 | **困难发生（黄金信号）** |
| 查询边界 | 复制操作、打开搜索框、切到浏览器 | 信息获取 |
| 显式标记 | 全局快捷键 Ctrl+Alt+M | 人在回路 |

**技术实现**：
- macOS：`pyobjc` + AX API
- Windows：`pywinauto` / `uiautomation` + UIA
- Linux：`atspi` + `evdev`
- 浏览器：Chrome/Edge 扩展拿 DOM 事件

**事件 schema**：
```json
{
  "ts": "2026-04-07T10:23:14",
  "type": "decision_boundary",
  "app": "Outlook",
  "context": {
    "window": "Re: Order #4471 refund",
    "focused_element": "Approve button"
  },
  "preceding_pause_ms": 8200,
  "novelty_score": 0.87
}
```

**关键机制**：`novelty_score` 由本地小型 n-gram 模型在线计算。低于阈值的事件**直接丢弃，不落盘**。这是预测编码原则的实现。

**预期输出量**：~几百 KB / 天 / 员工。无截屏。

---

### 3.3 L2 — Episode Builder（情节构建层）

**职责**：把 L1 的稀疏事件聚合成有头有尾的情节（episode），每条情节对应一次有意义的工作单元。

**理论依据**：Zacks (2007) event segmentation theory——大脑在预测误差峰值处自动切分事件边界。

**切分规则**：
- **起点**：上一个"任务边界"事件
- **终点**：下一个"决策边界"事件，或超过 N 分钟无新事件
- **提交触发**：情节中至少包含一个高 novelty_score 的"异常边界"事件，或员工显式标记
- **不满足条件 → 直接丢弃**

**仅在情节被提交时**才触发高分辨率上下文捕获：截图 + accessibility tree + 剪贴板 + 最近 30 秒环形缓冲。这是整个系统**唯一会产生大文件的地方**。

**Episode schema**：
```json
{
  "episode_id": "ep_20260407_001",
  "goal_hint": "处理订单 #4471 的退款请求",
  "duration_s": 142,
  "trigger": "long_pause + undo_burst",
  "events": [...],
  "snapshot": "ep_20260407_001.png",
  "tacit_signals": {
    "pauses": [{"at": 8.2, "before": "Approve"}],
    "undos": 2,
    "external_lookups": ["搜索 'Stornorichtlinie >50€'"]
  }
}
```

**`tacit_signals` 是这一层的精髓**——长停顿、撤销、外部查询，这三件事是隐性知识浮出水面的物理痕迹。L3 会专门盯着这些信号去问问题。

**预期输出量**：几十条 episode / 天 / 员工，总量 MB 级。

---

### 3.4 L3 — Dialogue Engine（外化对话层）

**职责**：用反事实和对比追问，把员工的隐性判断逼成显式陈述。

**这是整个框架最关键的一层**，也是 Skill Harvester 与所有 RPA / task mining 工具的根本差别。前面已经论证过：**条件不能被观察，只能被询问**。

**触发时机**（绝不打断工作）：
- 每日尾声：下班前 5 分钟弹出轻量界面，列出今天的 3-5 条最高价值 episode
- 被动模式：员工没空时进队列，攒到周末或主动打开时再问
- **永远不打断正在进行的工作**——hard rule

**追问模板**（由 LLM 根据 `tacit_signals` 自动生成）：

| 信号 | 追问类型 | 示例 |
|---|---|---|
| 长停顿在某动作前 | 决策点询问 | "你在按 Approve 前停了 8 秒，那一刻你在权衡什么？" |
| 撤销 | 反事实询问 | "你先选了 Escalate 又改成 Approve，是因为看到了什么？" |
| 外部查询 | 知识缺口询问 | "你查了退货政策，是这单有什么特殊？以后什么情况下你也会去查？" |
| 多 episode 走不同分支 | **对比询问** ⭐ | "上周一单 60€ 你直接批了，今天 55€ 你升级了，差别在哪？" |

**回答形式**：语音（ASR 转录）或两三句文字，挂在对应 episode 上，形成 `(动作, 隐性条件)` 训练对。

**这一步的本质**：把 ACT-R 的 compilation **逆向跑一遍**——把已经沉到自主阶段的知识临时拉回认知阶段，让它能被语言捕获。

---

### 3.5 L4 — Production Inducer（产生式归纳层）

**职责**：把 L3 的"动作 + 外化条件"对归纳成参数化 IF-THEN 规则集。

**运行方式**：离线批处理，每周或每攒够 N 条 episode 跑一次。

**流程**：
1. **聚类**：按 `goal_hint` 把 episode 聚成族（如所有"处理退款"类）
2. **分裂检测**：在同族内找出"走了不同动作分支"的 episode 对，这些就是产生式分裂点
3. **条件归纳**：把分裂双方的上下文 + 员工对话回答喂给 LLM，输出候选规则
4. **MDL 评估**：优选"短规则 + 高覆盖率"的规则集，剃掉低性价比规则
5. **冲突解决**：按 Anderson 的 utility 分数排序（成功使用 / 总匹配）

**输出格式**（中间表示）：
```
PRODUCTION refund_auto_approve
  GOAL:      process_refund
  CONDITIONS:
    - order_amount < 50 EUR
    - customer_tenure > 6 months
    - no_prior_disputes
  ACTIONS:
    - click(Approve)
    - log(reason = "small_amount_trusted_customer")
  UTILITY: 0.94 (47/50)
  SOURCE_EPISODES: [ep_20260301_003, ep_20260315_007, ...]
```

---

### 3.6 L5 — Skill Compiler（Skill 编译层）

**职责**：把 L4 的产生式集合编译成 SKILL.md，放进现有 skills 目录。

**这一层是 `skill-creator` 的逆向调用**——前者把人类描述变成 SKILL.md，这一层把机器归纳的产生式变成 SKILL.md。两者最终汇入同一个目录，对下游 Agent 完全透明。

**具体工作**：
1. 同一 goal 下的所有产生式整理成 IF-THEN 段落
2. 自动生成 description 和触发关键词（满足 Cowork Skill 触发机制）
3. 动作侧映射到现有工具调用（computer-use MCP / Chrome MCP / 已连接 API）
4. 每条规则标注：**来源 episode 链接 + 置信度 + 覆盖样本数**
5. 草稿放进 `skills/pending/` 目录，**等待人类 review**——hard rule

---

## 4. 横切关注（非功能性约束）

### 4.1 隐私与合规
- L1 支持按应用 / 时间段 / 窗口标题白黑名单
- 所有数据**本地存储**，绝不出本机
- L2 截图前跑 OCR + PII 过滤器（信用卡号、密码字段、身份证号自动打码）
- 德国语境下：部署前必须有 Betriebsrat 协议，符合 GDPR + BetrVG §87
- 托盘常驻"暂停录制"开关：30 分钟 / 一天 / 永久

### 4.2 可审计性
每条进入正式 `skills/` 目录的规则必须能反向追溯到原始 episode + 截图 + 员工对话。专家 review 时点开任何规则都能看到"基于哪 5 条 episode 归纳出来的"。

### 4.3 冷启动友好
前两周完全没有产生式产出是正常的——系统在攒 episode。这个预期必须显式告诉用户。

### 4.4 信息压缩比对照表

| 层 | 对应认知机制 | 压缩 |
|---|---|---|
| L1 | 注意力门控 / 预测编码 | 10^7 → 10^4 bit/s |
| L2 | 海马体稀疏写入 / 事件分割 | 几十条 episode/天 |
| L3 | 反事实推理 / CTA | 隐性 → 显式 |
| L4 | ACT-R compilation + MDL | episode → 规则 |
| L5 | 程序性知识可读外化 | 规则 → SKILL.md |

最终复杂度：**O(困难瞬间数 × 平均 episode 大小) + O(规则数 × 平均规则长度)**，与员工总工时无关。

---

## 5. MVP 路线图：反向开发

**核心建议**：不要先做 L1/L2。从 L5 倒着做。

### 5.1 为什么反向开发
L1/L2 工程量最大（系统级 hook、跨平台、隐私合规），但它们解决的只是"如何高效采集"。**采集可以先用最笨的方式凑合**——让员工手动按快捷键提交"刚才那一段值得记录"，配一次截图 + 一句话描述。这就是一个可用 episode，足够喂给 L3。

先把 **L3 → L4 → L5** 跑通，证明"从 20 条手动 episode 能编译出一条像样的 SKILL.md"，再回头做 L1/L2 自动化。

### 5.2 三阶段计划

| 阶段 | 周期 | 产物 | 验收标准 |
|---|---|---|---|
| **Phase 1: 闭环原型** | 1-2 周 | L3+L4+L5 + 手动 episode 提交工具 | 从 20 条手动 episode 编译出 1 条可用 SKILL.md |
| **Phase 2: 半自动采集** | 3-4 周 | L2 + 简化版 L1（仅订阅几个核心事件源） | 一个员工一周自动产出 ~30 条 episode |
| **Phase 3: 全栈采集 + 治理** | 6-8 周 | 完整 L1 + 隐私合规层 + 审计 UI | 通过 GDPR 自查 + 员工 review 流程 |

---

## 6. 与现有 Cowork 体系的关系

```
                  ┌──────────────┐
                  │ skill-creator│  ← 人类直接写 Skill
                  └──────┬───────┘
                         │
                         ▼
                  ┌──────────────┐
                  │  skills/     │  ← Agent 调用入口
                  └──────▲───────┘
                         │
                  ┌──────┴───────┐
                  │ skill-harvester│ ← 从员工行为编译 Skill
                  └──────────────┘
```

Skill Harvester 不替换 skill-creator，而是**扩展知识来源**：让 Skill 既可以由人类手写，也可以从员工日常工作中自动归纳。两者最终汇入同一个目录，对 Agent 完全透明。

---

## 7. 核心信念

> **Skill 不是行为的录音，而是判断的化石。**

Skill Harvester 的工作不是看员工的手，而是**蹲守员工那些不得不停下来想一想的瞬间**，用对话把那一下的思考从他脑子里取出来，编译成可以被任何 Agent 复用的 IF-THEN 规则。

这就是把 ACT-R 的 compilation 倒过来跑——人脑把陈述性压成程序性，我们把程序性再拉回陈述性，并保留它的可执行形式。这种"既隐性又可读"的中间态，恰好就是 SKILL.md 的本质。

---

## 8. 待决问题（Open Questions）

1. **L3 对话频率**：每天 5 分钟会不会让员工烦？需要 A/B 测试。
2. **L4 归纳的最小样本数**：多少条 episode 才足以归纳出一条可信规则？经验值待校准。
3. **跨员工知识合并**：两个员工对同一情境给出不同规则时如何裁决？需要专家仲裁层。
4. **Skill 漂移检测**：界面改版后旧 Skill 失效，如何自动发现并触发重新采集？
5. **冷启动加速**：能否用现有 SKILL.md 反向生成"我们已经认识哪些产生式"的先验，让 L1 跳过这些已知模式？

---

*本文档基于一段关于"如何从认知科学和逻辑学角度理解 Skill"的讨论编译而成。所有设计选择都可追溯到 Polanyi 的隐性知识理论、Anderson 的 ACT-R 框架、Klein 的 RPD 模型、Zacks 的事件分割理论，以及 Shannon 的信息论。*
