# Skill Harvester — 项目设计文档 v3

> **v3 修订说明**:针对 v2 review 中的五条核心批评(黄金时刻悖论 / 微观记忆衰减 / 因果链断裂 / 过度线性化 / L0 自举悖论),做了四处具体修订。完整 changelog 见 [CHANGELOG.md](./CHANGELOG.md)。
>
> v3 不推翻 v2 的六层架构,只是在三个层(L0 / L1 / L3)各打了一个补丁,并新增 §0.1 一句话定位以预防误读。

---

## 0. 系统的核心信念

> **Skill 不是行为的录音,而是判断的化石。**

把 ACT-R 的 compilation 倒过来跑 —— 人脑把陈述性知识压成程序性知识,我们把程序性知识再拉回陈述性知识,并保留它的可执行形式。这种"既隐性又可读"的中间态,恰好就是 SKILL.md 的本质。

### 0.1 SKILL.md 是给谁用的【v3 新增】

**SKILL.md 是 LLM Agent 的"上下文先验",不是传统执行引擎的硬规则。**

- Skill Harvester 不试图穷尽专家直觉
- Skill Harvester 只试图把"专家在这个场景里关心哪几个维度"显式化
- 表达力损失由下游 LLM Agent 的语言理解能力兜底
- 因此"IF-THEN 必然脆弱"这一类批评是**对硬规则系统成立、对 LLM prior 不成立**

### 0.2 Skill Harvester 不做什么(non-goals)【v3 新增】

明确**不适合**用 Skill Harvester 提取的任务类型:

- **高度依赖多模态直觉的判断**(语气微表情、视觉模式识别、审美) — IF-THEN 表达力天花板真实存在
- **强实时控制循环**(交易、操作系统调度) — 反事实对话延迟无法满足
- **需要精确执行而非判断**(财务对账的金额一致性) — 这类任务不属于"产生式"范畴,RPA 更合适
- **极度个性化的一次性创作**(写诗) — 没有可归纳的 IF-THEN 结构

---

## 1. 系统成立的前置假设(v3 增补)

| # | 假设 | 证伪信号 | v3 变化 |
|---|---|---|---|
| A1 | 员工能写出动宾结构的日常任务清单 | 写不出或写空泛 | — |
| A2 | LLM 能在小样本下做高置信度 goal 分类 | 高置信度子集占比 < 50% | — |
| A3 | 员工事后追问能给出有信息量的回答 | 主观分 < 3 | **拆为 A3a / A3b** |
| **A3a** | **immediate 层(0-30s)的"一句话快问"能获得高质量瞬时记忆** | 员工自愿触发率 < 10% 或回答低于 3 分 | **v3 新增** |
| **A3b** | **deferred 层(当晚)的"对比追问"不依赖瞬时记忆** | 员工答非所问或反复说"我记不清了" | **v3 新增** |
| A4 | 5-10 条同类 episode 能归纳出可信规则 | 第二位员工无法认可 | — |
| A5 | SKILL.md 让 Agent 一致率显著提升 | A/B 差距 < 20pp | — |
| **A6** | **routine confirmation 触发器能在不打扰的前提下捕获已经压实的产生式** | 频率检测召回率 < 50% 或员工反感 | **v3 新增** |

A3 拆分和 A6 新增是针对 v2 review 的批评 1 和 2。

---

## 2. 问题陈述

(同 v2,略 — 见 [v2 §2](./skill-harvester-design-v2.md#2-问题陈述))

---

## 3. 核心设计原则(v3 修订)

1. **信息 = 惊讶度**(Surprisal) — Phase 1 主信号
2. **【v3 新增】频率 = 化石** — 高频低 surprisal 模式本身就是产生式存在的证据,这是与 surprisal **正交的第二条触发线**
3. **困难是黄金信号**(Klein RPD)
4. **【v3 修订】流畅是另一种黄金信号** — 已经压实的产生式没有停顿,只能靠频率发现
5. **条件不能被观察,只能被询问**(Polanyi)
6. **【v3 修订】问的时机决定问的质量** — 瞬时记忆问题必须 immediate,规律记忆问题可以 deferred
7. **奥卡姆剃刀的算法形式 = MDL**
8. **机器编译必须保留可读性**
9. **Goal 不靠猜,靠先验 + 询问**(L0)
10. **【v3 强化】L0 是动态生长的,不是一次性产物** — 这是对"自举悖论"质疑的预防性回答
11. **意图与执行解耦**(L4 双层)

---

## 4. 系统架构(v3 修订)

### 4.1 六层架构 + 两条触发线

```
┌─────────────────────────────────────────────────┐
│  L5  Skill Compiler          → SKILL.md         │
├─────────────────────────────────────────────────┤
│  L4  Production Inducer      → IF-THEN 规则集    │
├─────────────────────────────────────────────────┤
│  L3  Dialogue Engine         → 反事实问答记录     │
│       step 0: goal 自动分类                      │
│       step 1: immediate 一句话快问 (新增)        │
│       step 2: deferred 对比追问                  │
├─────────────────────────────────────────────────┤
│  L2  Episode Builder         → 困难瞬间 + 流畅瞬间 │
│       trigger A: surprisal (困难)                │
│       trigger B: routine confirmation (流畅,新增)│
├─────────────────────────────────────────────────┤
│  L1  Sparse Sensor                              │
│       sensor A: 异常事件                         │
│       sensor B: 频率累加器 (新增)                │
│       buffer:   recent_metadata 长时缓冲 (新增)  │
├─────────────────────────────────────────────────┤
│  L0  Task Catalog (动态生长)                     │
└─────────────────────────────────────────────────┘
```

**v3 的三处加粗**:
- L1 加 **频率累加器** + **长时元数据缓冲**
- L2 加 **routine confirmation 触发器**(第二条触发线)
- L3 加 **immediate 一句话快问**(瞬时记忆专用)

---

### 4.2 L0 — Task Catalog(v3 强化:动态生长)

L0 的写法约束(动宾结构、5-15 项、必须 other 兜底)同 v2。**v3 把"动态生长"从 §4.2 中段提到本节开头**:

> **L0 不是一次性产物。** 系统从 day 1 起就在改写它:
> 1. **L4 反向建议拆分**:在某 task 内发现稳定分裂时,建议员工把 task 拆成两项
> 2. **L4 反向建议新增**:`other` 类下积累的 episode 出现规律时,建议员工新增 task
> 3. **routine 触发器反向建议**:观察到员工每天大量做"未在清单里"的某类动作时,建议员工补进 L0
> 4. **员工每周 5 分钟 review** 系统提出的清单修改建议

**所以 L0 不是"先把所有事情说清楚",而是"先给系统一张粗略地图,系统帮你画准"**。这直接回应 v2 review 的批评 5。

---

### 4.3 L1 — Sparse Sensor(v3 重大修订)

**职责**:三件事 —— 异常检测、频率累加、长时元数据缓冲。

#### 4.3.1 Sensor A:异常事件(同 v2)

捕获停顿、撤销、外部查询、显式标记 —— surprisal 信号。

#### 4.3.2 Sensor B:频率累加器【v3 新增】

**为什么需要**:v2 review 的批评 1 指出 —— 已经完全压实的产生式在执行时**没有任何 surprisal 信号**(心流状态),sensor A 完美错过这一类。但这一类恰恰是企业最想自动化的(高频、稳定、可复制)。

**怎么做**:
- 对每个 (active_app, window_pattern, action_signature) 三元组维护一个**指数衰减计数器**
- window_pattern 是窗口标题去掉数字/ID 后的模板(例如 "Re: Order #4471 refund" → "Re: Order #N refund")
- action_signature 是事件序列的 hash(例如 "open → scroll → click(approve)")
- 当某个三元组在过去 7 天内累计触发 ≥ N 次(默认 N=10)时,**标记为 routine candidate**,通知 L2

**关键设计**:
- 频率累加器**不存截图**,只存计数和时间戳。开销极低
- 计数器满足 N 次后,**只触发一次** routine confirmation episode,然后进入冷却期(默认 30 天)
- 冷却期内同一模式的执行不再触发,避免反复打扰

#### 4.3.3 长时元数据缓冲【v3 新增,回应批评 3】

**为什么需要**:v2 review 的批评 3 指出 —— 决策的"因"可能在 3 分钟前,30 秒环形缓冲不够。

**怎么做**:
- L1 持续维护一个**滚动 15 分钟**的元数据队列
- 队列里只存:`(timestamp, app, window_title_template, url, event_type)`,**没有截图、没有像素、没有完整文本**
- 单条记录 < 200 字节,15 分钟全量 < 100 KB,常驻内存
- episode 触发时,把这 15 分钟的元数据序列**整体打包进 episode**

**Episode schema 加段**(v3):
```json
{
  ...,
  "recent_metadata": [
    {"ts": "10:20:11", "app": "Outlook", "window_template": "Re: Order #N refund", "event": "open"},
    {"ts": "10:21:03", "app": "Outlook", "window_template": "Inbox - VIP customer", "event": "open"},
    {"ts": "10:22:45", "app": "ERP",     "window_template": "Refund Detail #N",     "event": "focus"},
    {"ts": "10:23:14", "app": "ERP",     "window_template": "Refund Detail #N",     "event": "long_pause"}
  ]
}
```

L3 的反事实追问 prompt 里就可以塞进这一段,问员工:"3 分钟前你看了一封 'Inbox - VIP customer' 的邮件,这跟刚才的犹豫有关吗?" —— 因果窗口从 30 秒扩到 15 分钟,成本几乎为零。

---

### 4.4 L2 — Episode Builder(v3 修订:两条触发线)

**v3 修订**:L2 不再只听一种触发器,有两条独立的提交路径。

#### 触发线 A:困难提交(同 v2)

由 L1 sensor A 触发。捕获正在分裂的产生式。

#### 触发线 B:routine confirmation 提交【v3 新增】

由 L1 sensor B 触发。捕获已经压实的产生式。

**Episode 的 trigger 字段会标明**:`"trigger": "routine_confirmation"`,L3 拿到这种 episode 时会用**不同的追问模板**(见 4.5.2)。

**两条线的差别**:

| 维度 | 困难触发 | routine 触发 |
|---|---|---|
| 目标产生式 | 正在分裂的 | 已经压实的 |
| 物理信号 | 停顿/撤销/查询 | 高频流畅执行 |
| 触发频率 | 几十条/天 | 几条/周 |
| 追问问题 | "你刚才在权衡什么?" | "你按照什么规则判断?" |
| 员工的回答路径 | 反事实回忆(难) | 规则陈述(容易) |
| 记忆衰减敏感度 | 高 | 低 |

**这一对触发线在认知科学上对应 Anderson 的 ACT-R 三阶段**:routine 触发抓的是 autonomous 阶段的产生式(已经压实),困难触发抓的是 associative 阶段的(还在压实中)。两者合起来才覆盖完整的技能光谱。

---

### 4.5 L3 — Dialogue Engine(v3 重大修订:三时延 + 两路径)

**v3 把 L3 拆成三个时延档,按追问类型分发**。原 v2 的"下班前批处理"是错的 —— Ericsson & Simon 的研究告诉我们 retrospective verbalization 在分钟级以上质量大幅下降。

#### 4.5.1 三时延架构

| 时延档 | 触发方式 | 问什么 | 为什么这个时延 |
|---|---|---|---|
| **immediate**(0-30s) | episode 触发后立即在屏幕角落显示一个红点,**员工自愿**按热键弹出 | 一句话快问:"刚才那一下,在想什么?" — 文字或语音二选一 | 瞬时记忆只在此时窗口内可信 |
| **close-window**(几分钟) | 鼠标静止 30s + 无键盘输入时弹出 | 选择题式追问,LLM 基于 episode 特征生成 3-4 个候选 | 短期记忆,辅助回忆 |
| **deferred**(当晚 5 分钟) | 主动调用 `skill-harvester review` | 只做对比追问("上周一单 60€ 你批了,今天 55€ 你升级了,差别在哪") | 规律记忆,不依赖瞬时 |

**分时延的依据**:
- "你刚才在想什么" → 极度依赖瞬时记忆 → 必须 immediate
- "上周和今天为什么不同" → 依赖规律记忆 → 可以 deferred
- v2 把两者混在一档里是设计失误

#### 4.5.2 两条追问路径

针对 4.4 的两种触发,L3 用不同的追问模板:

**困难触发的 episode**(反事实路径):
> "你最终点了 Approve 之前停了 8 秒。最影响这个判断的是:
> A. 订单金额(55€)
> B. 客户历史
> C. 退货原因
> D. 其他 ___"

**routine 触发的 episode**(规则陈述路径):
> "我注意到你这周做了 23 次很相似的'退款审批'操作,几乎都在 5 秒内完成。能用一句话说说,你判断'可以直接批'的标准是什么?
> (这种问题不需要回忆某个具体瞬间,只需要描述你的固定做法)"

**关键差别**:routine 路径的问题**不要求员工回忆具体瞬间**,所以可以放进 deferred 时延档,完全不打扰工作。

#### 4.5.3 hard rule 的修订

v2 的 "永远不打断正在进行的工作" 改成:

> **永远不强制打断工作**。immediate 层用自愿触发的红点,员工不按就不弹;close-window 层只在自然停顿时弹;deferred 层完全异步。

---

### 4.6 L4 — Production Inducer(同 v2)

意图层 + 执行层双层结构,见 [v2 §4.6](./skill-harvester-design-v2.md#46-l4--production-inducerv2-修订)。

**v3 唯一变化**:归纳时把 episode 的 `recent_metadata` 也喂给 LLM,让它能识别"决策的因来自更早的窗口"这种模式。

---

### 4.7 L5 — Skill Compiler(同 v2)

输出格式同 v2。**v3 唯一变化**:在 SKILL.md 的 frontmatter 里加一行 `target: llm_agent_prior`,显式声明这份产物的消费方式(回应批评 4)。

---

## 5. 横切关注

(同 v2,略)

---

## 6. MVP 路线图(v3 修订)

### Phase 0:可行性证伪(1 周)— **新增两个验证项**

在 v2 的五个假设之外,Phase 0 还要验证:

- **A3a**:让员工在工作中按一个物理按钮触发"一句话快问",看自愿触发率和回答质量
- **A6**:手工模拟"频率累加器" —— 让员工每天做完后回看,标出"这件事我今天做了 N 次"的高频项,看这些高频项里能不能找到值得归纳的产生式

### Phase 1:闭环原型(2-3 周)— **加两个 stub**

在 v2 Phase 1 的范围之上,新增两个 **stub 模块**(只有最朴素实现,留接口):

- `routine_detector.py` — 实现 L1 频率累加器
- `tiered_dialogue.py` — 实现 L3 三时延框架(immediate 用 CLI 快问,close-window/deferred 沿用 v2 的 review 命令)

**Phase 1 验收**(v3 修订):
- v2 的端到端 A/B 一致率 +20pp(不变)
- **新增**:routine 触发器在 1 周内能找到至少 1 条高频模式,且员工能给出可用的规则陈述

### Phase 2 / Phase 3

(同 v2,见 [v2 §6](./skill-harvester-design-v2.md#6-mvp-路线图v2-修订))

---

## 7. v3 待决问题

1. routine 触发器的 N 阈值(7 天内 10 次?5 次?)和冷却期(30 天?)需要 Phase 0 标定
2. immediate 一句话快问用文字还是语音 —— 语音转录质量 vs 输入摩擦,需要 A/B
3. recent_metadata 的窗口长度(15 分钟够不够?)需要看 Phase 1 的因果链识别成功率
4. window_template 的归一化算法(数字/ID 抽取规则)需要按目标 app 调
5. 跨员工知识合并(留给 Phase 3)

---

## 附录 A:v3 相对 v2 的关键变化

| 变化 | 位置 | 来源 |
|---|---|---|
| §0.1 明确 SKILL.md 是 LLM prior 而非硬规则 | §0.1 | v2 review 批评 4 |
| §0.2 新增 non-goals 清单 | §0.2 | v2 review 批评 4 |
| §1 假设 A3 拆为 A3a / A3b | §1 | v2 review 批评 2 |
| §1 新增假设 A6 (routine 触发) | §1 | v2 review 批评 1 |
| §3 新增原则 "频率 = 化石" | §3 | v2 review 批评 1 |
| §3 新增原则 "问的时机决定问的质量" | §3 | v2 review 批评 2 |
| §3 强化原则 "L0 动态生长" | §3 | v2 review 批评 5 |
| §4.2 把 L0 动态性提到节首 | §4.2 | v2 review 批评 5 |
| §4.3 L1 新增 sensor B (频率累加器) | §4.3.2 | v2 review 批评 1 |
| §4.3 L1 新增长时元数据缓冲 | §4.3.3 | v2 review 批评 3 |
| §4.4 L2 第二条触发线 (routine confirmation) | §4.4 | v2 review 批评 1 |
| §4.5 L3 拆成三时延档 | §4.5.1 | v2 review 批评 2 |
| §4.5 L3 两条追问路径 (反事实 / 规则陈述) | §4.5.2 | v2 review 批评 1+2 |
| §4.5 hard rule 改 "永远不强制打断" | §4.5.3 | v2 review 批评 2 |
| §6 Phase 0 新增验证 A3a / A6 | §6 | 上面假设新增 |
| §6 Phase 1 新增 routine_detector + tiered_dialogue stub | §6 | 配合 v3 架构修订 |

---

*v3 基于 v2 + 五条核心 review 批评修订而成。*
